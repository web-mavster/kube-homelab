flags:
  rewrite: false

service:
  name: argo-cd-argocd-server
  port:
    number: 443

annotations:
  app.kubernetes.io/name: "argocd"

ingress:
  subdomain: argo
  routePath: /

hosts:
%{ for host in hosts ~}
- name: ${host}
%{ endfor ~}
